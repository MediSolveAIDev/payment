# 요청 012 구현 계획 — 구독 실패 시 결제 + 일반결제 취소(수수료)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PAST_DUE 구독의 수동결제를 허용하고, 일반(단건) 결제 취소(서비스별 가능여부·수수료율, 외부 API/Admin/샘플)를 추가한다.

**Architecture:** 취소 정책은 Service 컬럼(cancellation_enabled/fee_percent)에 저장. 취소는 토스 부분취소(환불=금액−수수료)로 실행하고 Payment에 canceled_amount/cancel_fee/canceled_at을 기록, 환불 집계는 canceled_amount 기준. 모델/마이그레이션 변경 + 도메인·라우트·샘플·매뉴얼. 변경 코드엔 주석 동반(프로젝트 표준).

**Tech Stack:** FastAPI, SQLAlchemy 2 async, Alembic, TossPayments, Jinja2, Django(샘플), pytest

**스펙:** `docs/superpowers/specs/2026-06-09-request-012-design.md`. 현재 마이그레이션 head: `c3d4e5f6a7b8`.

## 파일 구조
- `app/models/service.py`·`app/models/payment.py` + 마이그레이션 — 정책/취소 기록 컬럼 (Task 1)
- `app/services/subscriptions.py` — manual_charge PAST_DUE (Task 2)
- `app/toss/client.py`·`app/toss/fake.py` — cancel_payment (Task 3)
- `app/services/payments.py`·`audit_labels.py`·`dashboard.py`·`webhooks.py` — 취소 도메인+집계 (Task 4)
- `app/services/registry.py`·`app/admin/routes/services.py`·템플릿 — 정책 설정 (Task 5)
- `app/api/v1/payments.py`·`schemas/api.py`·`app/admin/routes/payments.py`·`payments/detail.html` — 취소 창구 (Task 6)
- `sample_service/shop/*` — 샘플 취소 (Task 7)
- `docs/claude/manual/*` — 매뉴얼 (Task 8)

---

### Task 1: 모델 컬럼 + 마이그레이션 (정책 + 취소 기록)

**Files:**
- Modify: `app/models/service.py`, `app/models/payment.py`
- Create: `alembic/versions/d4e5f6a7b8c9_cancel_policy_and_fields.py`
- Test: `tests/integration/test_models.py`(있으면) 또는 신규 단언

- [ ] **Step 1: Service 컬럼** — `app/models/service.py` `status` 컬럼 아래에 추가(`Boolean` import 추가):
```python
    cancellation_enabled: Mapped[bool] = mapped_column(
        Boolean, default=True, server_default="true")          # 단건결제 취소 허용 여부
    cancellation_fee_percent: Mapped[int] = mapped_column(
        default=0, server_default="0")                          # 취소 수수료율(0~100, %)
```
(`from sqlalchemy import Boolean, String` 로 보강.)

- [ ] **Step 2: Payment 취소 기록 컬럼** — `app/models/payment.py`에 추가(import에 `datetime` 확인):
```python
    canceled_amount: Mapped[int | None] = mapped_column(BigInteger, nullable=True)  # 실제 환불액(금액-수수료)
    cancel_fee: Mapped[int | None] = mapped_column(BigInteger, nullable=True)        # 차감 수수료
    canceled_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
```
(`BigInteger`, `DateTime` import 확인.)

- [ ] **Step 3: 마이그레이션** — `alembic/versions/d4e5f6a7b8c9_cancel_policy_and_fields.py`:
```python
"""cancel policy(services) + cancel fields(payments)

Revision ID: d4e5f6a7b8c9
Revises: c3d4e5f6a7b8
Create Date: 2026-06-09
"""
from typing import Sequence, Union
from alembic import op
import sqlalchemy as sa

revision: str = 'd4e5f6a7b8c9'
down_revision: Union[str, Sequence[str], None] = 'c3d4e5f6a7b8'
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column('services', sa.Column('cancellation_enabled', sa.Boolean(),
                  nullable=False, server_default='true'))
    op.add_column('services', sa.Column('cancellation_fee_percent', sa.Integer(),
                  nullable=False, server_default='0'))
    op.add_column('payments', sa.Column('canceled_amount', sa.BigInteger(), nullable=True))
    op.add_column('payments', sa.Column('cancel_fee', sa.BigInteger(), nullable=True))
    op.add_column('payments', sa.Column('canceled_at', sa.DateTime(timezone=True), nullable=True))


def downgrade() -> None:
    op.drop_column('payments', 'canceled_at')
    op.drop_column('payments', 'cancel_fee')
    op.drop_column('payments', 'canceled_amount')
    op.drop_column('services', 'cancellation_fee_percent')
    op.drop_column('services', 'cancellation_enabled')
```

- [ ] **Step 4: 단언 테스트** — `tests/integration/test_models.py`(없으면 신규)에:
```python
async def test_service_cancel_policy_defaults(db, cipher):
    from tests.factories import create_service
    svc, _, _ = await create_service(db, cipher)
    assert svc.cancellation_enabled is True
    assert svc.cancellation_fee_percent == 0
```

- [ ] **Step 5: 통과 확인** — Run: `uv run pytest tests/integration tests/unit -q` → PASS. `uv run alembic heads` → 단일 head `d4e5f6a7b8c9`.

- [ ] **Step 6: 커밋**
```bash
git add app/models/service.py app/models/payment.py alembic/versions/d4e5f6a7b8c9_cancel_policy_and_fields.py tests/integration/test_models.py
git commit -m "feat(012): Service 취소정책 + Payment 취소기록 컬럼 + 마이그레이션

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: PAST_DUE 수동 구독결제 허용

**Files:**
- Modify: `app/services/subscriptions.py`
- Test: `tests/integration/test_subscription_manage.py`(또는 manual_charge 테스트 위치)

- [ ] **Step 1: 실패 테스트** — manual_charge 테스트 파일에 PAST_DUE 허용 케이스 추가(기존 SUSPENDED 테스트 옆):
```python
async def test_manual_charge_allows_past_due(db, cipher, fake):
    from app.models import SubscriptionStatus
    svc, _, _ = await create_service(db, cipher)
    plan = await create_plan(db, svc)
    sub = await create_subscription(db, cipher, svc, plan, external_user_id="pd",
                                    status="PAST_DUE")
    result = await manual_charge_subscription(db, fake, cipher, service=svc,
                                              external_user_id="pd")
    assert result.status == SubscriptionStatus.ACTIVE   # 결제 성공 시 복귀
```
(픽스처/헬퍼는 기존 manual_charge 테스트 패턴 따름. fake가 성공 charge 반환.)

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest -k "manual_charge_allows_past_due" -x -q` → FAIL(현재 SUSPENDED만).

- [ ] **Step 3: 허용 상태 확장** — `app/services/subscriptions.py` `manual_charge_subscription`:
```python
    sub = await db.scalar(select(Subscription).where(
        Subscription.service_id == service.id,
        Subscription.external_user_id == external_user_id,
        Subscription.status.in_((SubscriptionStatus.SUSPENDED,
                                 SubscriptionStatus.PAST_DUE))))
    if sub is None:
        raise NotFoundError("정지/미수 상태의 구독을 찾을 수 없습니다")
```
docstring을 "SUSPENDED 또는 PAST_DUE(실패중) 구독의 수동 결제 — 성공 시 ACTIVE 복귀"로 갱신. 나머지(빌링키 확인·결제·복귀·기간) 로직 변경 없음.

- [ ] **Step 4: 통과 확인** — Run: `uv run pytest tests/integration -k "manual_charge" -q` → PASS(SUSPENDED 기존 + PAST_DUE 신규). 이어서 `uv run pytest -q`.

- [ ] **Step 5: 커밋**
```bash
git add app/services/subscriptions.py tests
git commit -m "feat(012): 수동 구독결제를 PAST_DUE(실패중)에도 허용

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 토스 결제 취소 API

**Files:**
- Modify: `app/toss/client.py`, `app/toss/fake.py`

- [ ] **Step 1: Protocol + Http 구현** — `app/toss/client.py`
  Protocol에 추가: `async def cancel_payment(self, payment_key: str, reason: str, *, cancel_amount: int | None = None) -> dict: ...`
  `HttpTossClient`에 구현(`charge` 근처):
```python
    async def cancel_payment(self, payment_key: str, reason: str,
                             *, cancel_amount: int | None = None) -> dict:
        """승인된 결제를 취소(환불)한다. cancel_amount 지정 시 부분취소.

        POST /v1/payments/{paymentKey}/cancel. 부분취소면 cancelAmount(환불액)를 보낸다.
        전액취소(cancel_amount=None)는 cancelReason만 전송한다.
        """
        body = {"cancelReason": reason}
        if cancel_amount is not None:
            body["cancelAmount"] = cancel_amount
        return await self._request(
            "POST", f"/v1/payments/{quote(payment_key, safe='')}/cancel", json=body)
```
(`_request`가 dict 반환인지 확인 — charge는 `_charge_result(data)`로 감쌈. cancel은 raw dict 반환으로 충분. `quote` 이미 import.)

- [ ] **Step 2: Fake 구현** — `app/toss/fake.py`에 추가(`__init__`에 `self.canceled: list = []`, `self.fail_cancel_with: TossError | None = None` 추가):
```python
    async def cancel_payment(self, payment_key, reason, *, cancel_amount=None):
        if self.fail_cancel_with is not None:
            raise self.fail_cancel_with
        self.canceled.append({"payment_key": payment_key, "reason": reason,
                              "cancel_amount": cancel_amount})
        return {"paymentKey": payment_key, "status": "CANCELED",
                "cancelAmount": cancel_amount}
```

- [ ] **Step 3: 통과 확인** — Run: `uv run python -c "import app.main"` 정상. `uv run pytest tests/integration tests/unit -q` → 기존 PASS(회귀 없음).

- [ ] **Step 4: 커밋**
```bash
git add app/toss/client.py app/toss/fake.py
git commit -m "feat(012): 토스 결제 취소 API(cancel_payment) + fake

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 취소 도메인 `cancel_one_off_payment` + 집계/웹훅

**Files:**
- Modify: `app/services/payments.py`, `app/admin/audit_labels.py`, `app/services/dashboard.py`, `app/services/webhooks.py`
- Test: `tests/integration/test_payment_cancel.py`(신설)

- [ ] **Step 1: 실패 테스트** — `tests/integration/test_payment_cancel.py`:
```python
"""단건 결제 취소 통합 테스트 (요청 012)."""
import pytest
from sqlalchemy import select

from app.core.errors import ConflictError, NotFoundError, PaymentFailedError
from app.models import Payment, PaymentKind, PaymentStatus, PaymentType
from app.services import payments as payment_service
from app.toss.errors import TossError
from app.toss.fake import FakeTossClient
from tests.factories import create_service


@pytest.fixture
def fake():
    return FakeTossClient()


async def _done_oneoff(db, svc, *, order="oc-1", amount=10000):
    p = Payment(subscription_id=None, service_id=svc.id, external_user_id="u",
                order_id=order, amount=amount, payment_type=PaymentType.ONE_OFF,
                kind=PaymentKind.ONE_OFF, status=PaymentStatus.DONE,
                idempotency_key=order, toss_payment_key=f"pay_{order}")
    db.add(p); await db.commit(); await db.refresh(p)
    return p


async def test_cancel_full_refund_no_fee(db, cipher, fake):
    svc, _, _ = await create_service(db, cipher)   # fee_percent 기본 0
    p = await _done_oneoff(db, svc, amount=10000)
    out = await payment_service.cancel_one_off_payment(
        db, fake, service=svc, order_id="oc-1", reason="테스트")
    assert out.status == PaymentStatus.CANCELED
    assert out.canceled_amount == 10000 and out.cancel_fee == 0
    assert fake.canceled and fake.canceled[0]["cancel_amount"] is None   # 전액취소


async def test_cancel_partial_with_fee(db, cipher, fake):
    svc, _, _ = await create_service(db, cipher)
    svc.cancellation_fee_percent = 10; await db.commit()
    p = await _done_oneoff(db, svc, order="oc-2", amount=10000)
    out = await payment_service.cancel_one_off_payment(
        db, fake, service=svc, order_id="oc-2", reason="테스트")
    assert out.cancel_fee == 1000 and out.canceled_amount == 9000
    assert fake.canceled[0]["cancel_amount"] == 9000                     # 부분취소


async def test_cancel_disabled(db, cipher, fake):
    svc, _, _ = await create_service(db, cipher)
    svc.cancellation_enabled = False; await db.commit()
    await _done_oneoff(db, svc, order="oc-3")
    with pytest.raises(PaymentFailedError):
        await payment_service.cancel_one_off_payment(
            db, fake, service=svc, order_id="oc-3", reason="x")


async def test_cancel_rejects_non_done_or_other_service(db, cipher, fake):
    svc, _, _ = await create_service(db, cipher)
    other, _, _ = await create_service(db, cipher, name="타서비스")
    p = await _done_oneoff(db, svc, order="oc-4")
    with pytest.raises(NotFoundError):
        await payment_service.cancel_one_off_payment(
            db, fake, service=other, order_id="oc-4", reason="x")
```

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/integration/test_payment_cancel.py -x -q` → FAIL(함수 없음).

- [ ] **Step 3: 취소 서비스 구현** — `app/services/payments.py`에 추가(import: `ConflictError`, `NotFoundError`는 errors에서; `from app.toss.errors import TossError` 이미 있음):
```python
async def cancel_one_off_payment(db: AsyncSession, toss: TossClient, *,
                                 service: Service, order_id: str, reason: str) -> Payment:
    """단건(ONE_OFF) DONE 결제를 취소(환불)한다.

    서비스 정책(cancellation_enabled)이 꺼져 있으면 취소 불가. 수수료율이 있으면
    환불액 = 금액 − (금액 × 수수료% // 100)으로 부분취소하고 수수료는 서비스가 차감한다.
    성공 시 status=CANCELED + canceled_amount/cancel_fee/canceled_at 기록.
    토스 취소 실패 시 상태는 DONE 유지(멱등 재시도 가능). 결제 3원칙과 달리 타임아웃
    PENDING 보존은 적용하지 않는다(취소는 재호출이 안전).
    """
    payment = await db.scalar(select(Payment).where(Payment.order_id == order_id))
    if payment is None or payment.service_id != service.id:
        raise NotFoundError("결제를 찾을 수 없습니다")
    if payment.kind != PaymentKind.ONE_OFF or payment.status != PaymentStatus.DONE:
        raise ConflictError("취소할 수 없는 결제입니다")
    if not service.cancellation_enabled:
        raise PaymentFailedError("취소가 허용되지 않는 서비스입니다", code="CANCEL_DISABLED")
    fee = payment.amount * service.cancellation_fee_percent // 100
    refund = payment.amount - fee
    try:
        if refund > 0:
            # refund == 금액이면 전액취소(cancelAmount 생략), 아니면 부분취소
            await toss.cancel_payment(
                payment.toss_payment_key, reason,
                cancel_amount=(None if refund == payment.amount else refund))
    except TossError as exc:
        await record_audit(db, actor_type="SERVICE", actor_service_id=service.id,
                           action="payment.cancel_failed", target_type="payment",
                           target_id=str(payment.id), detail={"code": exc.code})
        await db.commit()
        raise PaymentFailedError(f"취소 실패: {exc.message}", code=exc.code) from exc
    payment.status = PaymentStatus.CANCELED
    payment.canceled_amount = refund
    payment.cancel_fee = fee
    payment.canceled_at = utcnow()
    await record_audit(db, actor_type="SERVICE", actor_service_id=service.id,
                       action="payment.canceled", target_type="payment",
                       target_id=str(payment.id), detail={"refund": refund, "fee": fee})
    await db.commit()
    return payment
```
import 보강: `from app.core.errors import ConflictError, InputValidationError, NotFoundError, PaymentFailedError`(NotFoundError·ConflictError 추가).

- [ ] **Step 4: 감사 라벨** — `app/admin/audit_labels.py` ACTION_LABELS에:
```python
    "payment.canceled": "결제 취소",
    "payment.cancel_failed": "결제 취소 실패",
```

- [ ] **Step 5: 환불 집계 정확화** — `app/services/dashboard.py`
  `_refund_between`의 합계를 canceled_amount 우선으로:
```python
    q = select(func.coalesce(func.sum(
        func.coalesce(Payment.canceled_amount, Payment.amount)), 0)).where(
        Payment.status == PaymentStatus.CANCELED,
        Payment.requested_at >= start, Payment.requested_at < end)
```
  `_service_revenue`의 refund 서브쿼리도 `func.sum(func.coalesce(Payment.canceled_amount, Payment.amount))`로. 주석에 "부분환불은 canceled_amount 반영" 추가.

- [ ] **Step 6: 웹훅 전액취소 기록** — `app/services/webhooks.py` `_handle_payment_status_changed`에서 `payment.status = PaymentStatus.CANCELED` 설정 시 함께:
```python
        payment.status = PaymentStatus.CANCELED
        payment.canceled_amount = payment.amount   # 외부 전액취소 동기화
        payment.canceled_at = utcnow()
```
(`utcnow` import 확인.)

- [ ] **Step 7: 통과 확인** — Run: `uv run pytest tests/integration/test_payment_cancel.py tests/integration/test_dashboard.py tests/integration/test_webhooks.py -q` → PASS. 이어서 `uv run pytest -q`.

- [ ] **Step 8: 커밋**
```bash
git add app/services/payments.py app/admin/audit_labels.py app/services/dashboard.py app/services/webhooks.py tests/integration/test_payment_cancel.py
git commit -m "feat(012): 단건결제 취소 도메인 + 환불 집계(canceled_amount) + 웹훅 동기화

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 서비스 취소 정책 설정 (registry + Admin 폼)

**Files:**
- Modify: `app/services/registry.py`, `app/admin/routes/services.py`, `app/admin/templates/services/new.html`, `app/admin/templates/services/detail.html`(편집 폼)
- Test: `tests/e2e/test_admin_operations.py` 또는 service 관련 e2e

- [ ] **Step 1: 실패 e2e** — 서비스 e2e에 정책 저장 검증 추가(기존 서비스 생성/수정 테스트 패턴 따름):
```python
async def test_service_cancel_policy_form(client, db, redis_client, cipher):
    admin, pw = await create_user(db, role="SYSTEM_ADMIN")
    await admin_login(client, admin.email, pw)
    # 서비스 생성 시 취소정책 지정 (폼 필드명: cancellation_enabled, cancellation_fee_percent)
    # 생성/수정 라우트의 실제 폼 필드·CSRF 패턴에 맞춰 호출
    ...
    svc = (await db.scalars(select(Service).where(Service.name == "정책서비스"))).first()
    assert svc.cancellation_fee_percent == 15
```
(구현자는 기존 서비스 생성/수정 e2e를 보고 동일 폼 제출 방식으로 작성. 검증: 저장된 값.)

- [ ] **Step 2: registry 반영** — `app/services/registry.py`
  `register_service` 시그니처에 `cancellation_enabled: bool = True, cancellation_fee_percent: int = 0` 추가, Service 생성 시 설정 + 0~100 검증:
```python
    if not 0 <= cancellation_fee_percent <= 100:
        raise InputValidationError("취소 수수료율은 0~100 사이여야 합니다")
```
  신규 `update_cancel_policy(db, service_id, *, enabled: bool, fee_percent: int, actor_user_id=None)`(0~100 검증 + 감사 `service.cancel_policy_updated` 또는 기존 service.updated 패턴). docstring 포함.
  audit_labels에 필요한 라벨 추가(없으면).

- [ ] **Step 3: Admin 라우트/폼** — `app/admin/routes/services.py`의 생성(services_create)·수정 핸들러에서 `cancellation_enabled`(체크박스 "on") + `cancellation_fee_percent`(int) 파싱 → registry 호출. `services/new.html`·`detail.html` 편집 폼에 입력 추가:
```html
  <label><input type="checkbox" name="cancellation_enabled" {{ 'checked' if service.cancellation_enabled }}> 일반결제 취소 허용</label>
  <label>취소 수수료(%) <input type="number" name="cancellation_fee_percent" min="0" max="100" value="{{ service.cancellation_fee_percent }}"></label>
```
(new.html은 기본 체크/0. 실제 폼 구조·CSRF에 맞춰 배치.)

- [ ] **Step 4: 통과 확인** — Run: `uv run pytest tests/e2e -q` → PASS(서비스 생성/수정 회귀 + 정책 저장). `uv run pytest -q`.

- [ ] **Step 5: 커밋**
```bash
git add app/services/registry.py app/admin/routes/services.py app/admin/templates/services tests
git commit -m "feat(012): 서비스 취소 정책(허용/수수료율) 설정 + Admin 폼

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 취소 창구 — 외부 API + Admin 결제상세

**Files:**
- Modify: `app/api/v1/payments.py`, `app/schemas/api.py`, `app/admin/routes/payments.py`, `app/admin/templates/payments/detail.html`
- Test: `tests/e2e/test_admin_operations.py`, `tests/e2e/test_api_endpoints.py`(존재 시)

- [ ] **Step 1: 실패 e2e (외부 API + admin)** — 적절한 e2e 파일에:
```python
async def test_external_cancel_api(client, db, cipher):
    # 서비스 인증 헤더 + DONE ONE_OFF 결제 시드 후 POST /api/v1/payments/{order_id}/cancel
    # → 200, status CANCELED. (HMAC 서명 헬퍼는 기존 api 테스트 패턴 사용)
    ...

async def test_admin_payment_cancel_button_and_action(client, db, redis_client, cipher):
    # ONE_OFF DONE 결제 + 취소허용 서비스 → 상세에 취소 버튼, POST /admin/payments/{id}/cancel → CANCELED
    # 취소불가 서비스 → "취소 불가" 노출, 버튼 없음
    ...
```
(구현자는 기존 api/admin 테스트 패턴으로 구체화. 핵심 단언: 외부 API 200+CANCELED, admin 취소 동작, 취소불가 노출.)

- [ ] **Step 2: 스키마** — `app/schemas/api.py`에:
```python
class OneOffCancelRequest(BaseModel):
    reason: str = Field(default="사용자 취소", max_length=200)
```

- [ ] **Step 3: 외부 API 라우트** — `app/api/v1/payments.py`:
```python
@router.post("/payments/{order_id}/cancel")
async def cancel_payment(order_id: str, payload: OneOffCancelRequest,
                         service: Service = Depends(payment_rate_limit),
                         db: AsyncSession = Depends(get_db),
                         toss: TossClient = Depends(get_toss)):
    """단건 결제 취소 — 서비스 정책에 따라 환불(수수료 공제). 문서 11 참조.

    payment_rate_limit: 토스 취소(환불) API 호출이 수반된다.
    """
    payment = await payment_service.cancel_one_off_payment(
        db, toss, service=service, order_id=order_id, reason=payload.reason)
    return PaymentResponse.model_validate(payment)
```
import에 `OneOffCancelRequest` 추가.

- [ ] **Step 4: Admin 취소 라우트** — `app/admin/routes/payments.py`:
```python
@router.post("/payments/{payment_id}/cancel")
async def payment_cancel(payment_id: uuid.UUID, request: Request,
                         ctx: AdminContext = Depends(require_any),
                         db: AsyncSession = Depends(get_db),
                         toss: TossClient = Depends(get_toss)):
    """Admin에서 단건 결제 취소(운영자). 스코프·CSRF 검증 후 도메인 호출."""
    await validate_csrf(request)
    payment = await db.get(Payment, payment_id)
    scope = service_scope(ctx)
    if payment is None or (scope is not None and payment.service_id not in scope):
        raise NotFoundError("결제를 찾을 수 없습니다")
    service = await db.get(Service, payment.service_id)
    await payment_service.cancel_one_off_payment(
        db, toss, service=service, order_id=payment.order_id, reason="관리자 취소")
    return RedirectResponse(f"/admin/payments/{payment_id}", status_code=303)
```
import 보강: `validate_csrf`, `get_toss`(from app.api.deps), `TossClient`, `payment_service`(app.services), `RedirectResponse`.

- [ ] **Step 5: 결제상세 템플릿** — `payments/detail.html`에 취소 영역(상태가 CANCELED면 환불/수수료 표시, DONE·ONE_OFF면 버튼/취소불가):
```jinja
{% if payment.kind == 'ONE_OFF' and payment.status == 'DONE' %}
  {% if service.cancellation_enabled %}
  <form method="post" action="/admin/payments/{{ payment.id }}/cancel"
        onsubmit="return confirm('이 결제를 취소(환불)하시겠습니까? 수수료 {{ service.cancellation_fee_percent }}% 차감')">
    <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
    <button class="btn btn-danger" type="submit">결제 취소</button>
  </form>
  {% else %}<span class="badge">취소 불가(서비스 정책)</span>{% endif %}
{% endif %}
{% if payment.status == 'CANCELED' and payment.canceled_amount is not none %}
<p class="muted">환불 {{ "{:,}".format(payment.canceled_amount) }}원 · 수수료 {{ "{:,}".format(payment.cancel_fee or 0) }}원</p>
{% endif %}
```
(csrf_token 컨텍스트 전달 방식은 기존 admin 폼과 동일하게. payment_detail render에 service 이미 전달됨 — fee/enabled 접근 가능.)

- [ ] **Step 6: 통과 확인** — Run: `uv run pytest tests/e2e -q` → PASS. `uv run pytest -q`.

- [ ] **Step 7: 커밋**
```bash
git add app/api/v1/payments.py app/schemas/api.py app/admin/routes/payments.py app/admin/templates/payments/detail.html tests
git commit -m "feat(012): 결제 취소 외부 API + Admin 결제상세 취소/취소불가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 샘플 shop 취소

**Files:**
- Modify: `sample_service/shop/payment_client.py`, `shop/views.py`, `shop/urls.py`, `shop/templates/shop/result.html`, `shop/tests.py`
- (샘플은 별도 git 저장소 — 거기서 커밋)

- [ ] **Step 1: client 메서드** — `payment_client.py`에:
```python
def cancel_one_off_payment(order_id: str, reason: str = "사용자 취소") -> dict:
    """단건 결제 취소 — POST /api/v1/payments/{order_id}/cancel."""
    return _request("POST", f"/api/v1/payments/{order_id}/cancel", {"reason": reason})
```

- [ ] **Step 2: 뷰 + URL** — `views.py`에 `oneoff_cancel_view`(POST, order_id 받아 client 호출, PaymentAPIError면 메시지 — 취소불가 포함), `urls.py`에 `path("pay/cancel", views.oneoff_cancel_view)`.
```python
def oneoff_cancel_view(request):
    user = _current_user(request)
    if user is None:
        return redirect("/")
    if request.method == "POST":
        order_id = request.POST.get("order_id", "")
        try:
            payment_client.cancel_one_off_payment(order_id)
            messages.success(request, "결제가 취소되었습니다")
        except PaymentAPIError as exc:
            messages.error(request, f"{exc.message} ({exc.code})")
        except Exception as exc:  # noqa: BLE001
            messages.error(request, f"구독서버에 연결할 수 없습니다: {exc}")
    return redirect("/pay")
```

- [ ] **Step 3: 결과 화면 버튼** — `result.html`의 일반결제 성공(payment) 영역에 취소 버튼:
```html
{% if payment and payment.status == 'DONE' %}
<form method="post" action="/pay/cancel">{% csrf_token %}
  <input type="hidden" name="order_id" value="{{ payment.order_id }}">
  <button class="btn btn-danger" type="submit">결제 취소</button>
</form>
{% endif %}
```

- [ ] **Step 4: 스모크 테스트** — `shop/tests.py`에 `oneoff_cancel_view` 라우트/뷰 스모크(로그인 필요 리다이렉트, mock client 호출).

- [ ] **Step 5: 검증·커밋(샘플 저장소)** — `.venv/bin/python manage.py test shop` PASS 후:
```bash
cd sample_service && git add shop && git commit -m "feat: 일반 결제 취소 데모 추가" && cd ..
```

---

### Task 8: 매뉴얼 동기화

**Files:** `docs/claude/manual/11-one-off-payment.md`(취소 섹션), `01-service-registry.md`(취소 정책), `06-subscription-manage.md`(PAST_DUE 수동결제), `09-dashboard-settlement.md`(환불=canceled_amount), `08-api-auth.md`·`00-overview.md`(취소 API)

- [ ] **Step 1: 갱신** — 각 문서에 현재 코드 기준으로 반영(코드 직접 읽어 검증):
  - 11: "단건 결제 취소"(정책·수수료 공제·부분취소·상태 전이·API/Admin/샘플) 섹션.
  - 01: 서비스 취소 정책 컬럼·Admin 폼.
  - 06: manual_charge가 SUSPENDED+PAST_DUE 허용.
  - 09: 환불 집계 = coalesce(canceled_amount, amount).
  - 08/00: `POST /api/v1/payments/{order_id}/cancel` 추가.
- [ ] **Step 2: 커밋**
```bash
git add docs/claude/manual
git commit -m "docs(manual): 요청012 반영(단건 취소·정책·PAST_DUE 수동결제·환불 집계)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 전체 검증 + 최종 리뷰
- [ ] **Step 1: 전체 테스트** — Run: `uv run pytest -q` → 전체 PASS. `uv run alembic heads` → 단일 `d4e5f6a7b8c9`.
- [ ] **Step 2: 잔여 확인**
  - `grep -rn "canceled_amount" app/services/dashboard.py` → 환불 집계 반영.
  - `grep -rn "cancel_one_off_payment\|/cancel" app/api app/admin` → API+admin 라우트 존재.
  - 샘플 테스트 통과(`.venv/bin/python manage.py test shop`).
- [ ] **Step 3: 최종 코드리뷰** — 취소 흐름(정책·수수료·부분취소·상태) + 동작 보존 + 주석 정확성.

## 변경하지 않는 것
- 구독 1건 유니크 인덱스, create_subscription 중복 차단, 결제 3원칙(생성/갱신), 첫결제/할인.
