# 요청 011 구현 계획 — 요금제 필터·정산 분리/상세·결제상세·대시보드 만료/색상

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 구독·결제·정산 목록에 서비스 연동 요금제 필터를 추가하고, 정산에 구독/일반 분리금액(건수)·일반결제 상세보기를 더하며, 공용 결제 상세 페이지와 대시보드 만료 도넛·enum 색 배지를 구현한다.

**Architecture:** 요금제 옵션은 공용 `plan_name_options` 헬퍼로 서비스 연동(선택 서비스의 요금제만). 요금제 필터는 구독결제만(Plan 조인 → ONE_OFF 자동 제외). 결제 상세는 공용 `/admin/payments/{id}`. 도넛은 전체 상태 카운트로 환원. 색상은 종류/유형 badge CSS 추가. 모델/마이그레이션 없음.

**Tech Stack:** FastAPI, SQLAlchemy 2 async, Jinja2 + htmx, CSS, pytest

**스펙:** `docs/superpowers/specs/2026-06-09-request-011-design.md`
**테스트:** `uv run pytest <경로> -q`

## 파일 구조
- `app/admin/filters.py` — `plan_name_options` 공용 헬퍼(신설)
- `app/admin/routes/plans.py` — 헬퍼 사용으로 리팩터
- `app/admin/routes/subscriptions.py` — 구독/결제 요금제 필터 + 결제 상세 라우트
- `app/admin/templates/subscriptions/_table.html`, `payments/list.html` — 요금제 select + (결제)상세 링크
- `app/static/admin.css` — 종류/유형 badge 색
- `app/services/dashboard.py` — 도넛 전체 상태 + EXPIRED 색
- `app/services/settlement.py` — `settlement_summary` plan_name + sub_count/one_off_count
- `app/admin/routes/settlement.py` + `settlement/index.html` — 요금제 필터·분리금액·일반결제 상세
- `app/admin/templates/payments/detail.html` — 결제 상세(신설)

---

### Task 1: 공용 `plan_name_options` 헬퍼 + plans 리팩터

**Files:**
- Create: `app/admin/filters.py`
- Modify: `app/admin/routes/plans.py`
- Test: `tests/e2e/test_admin_operations.py`(요금제 옵션 회귀)

- [ ] **Step 1: 헬퍼 작성** — `app/admin/filters.py`:
```python
"""목록 화면 공용 필터 옵션 빌더."""
import uuid

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models import Plan


async def plan_name_options(db: AsyncSession, scope: list[uuid.UUID] | None,
                            service_filter: str) -> list[tuple[str, str]]:
    """요금제명 드롭다운 옵션. 스코프 내 distinct, 서비스 선택 시 그 서비스의 요금제만."""
    q = select(Plan.name).distinct().order_by(Plan.name)
    if scope is not None:
        q = q.where(Plan.service_id.in_(scope))
    if service_filter:
        try:
            q = q.where(Plan.service_id == uuid.UUID(service_filter))
        except ValueError:
            pass
    return [("", "전체 요금제")] + [(n, n) for n in (await db.scalars(q)).all()]
```

- [ ] **Step 2: plans_list 리팩터** — `app/admin/routes/plans.py`
import 추가: `from app.admin.filters import plan_name_options`. 기존 `name_q` 빌드(라인 ~126-132)를:
```python
    plan_options = await plan_name_options(db, ctx.service_ids, service_filter)
```
로 교체(동작 동일). 미사용이 된 `name_q` 관련 코드 제거.

- [ ] **Step 3: 통과 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py -k plan -q` → PASS(요금제 목록 필터 회귀). 없으면 `uv run pytest tests/e2e -q`.

- [ ] **Step 4: 커밋**
```bash
git add app/admin/filters.py app/admin/routes/plans.py
git commit -m "refactor(admin): 요금제명 옵션 빌더 공용화(plan_name_options)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 구독 목록 요금제 필터

**Files:**
- Modify: `app/admin/routes/subscriptions.py`, `app/admin/templates/subscriptions/_table.html`
- Test: `tests/e2e/test_admin_operations.py`

- [ ] **Step 1: 실패 테스트 추가** — `tests/e2e/test_admin_operations.py` 끝에:
```python
async def test_subscriptions_plan_filter(client, db, redis_client, cipher):
    svc, _, _ = await create_service(db, cipher, name="요금제필터서비스")
    p_basic = await create_plan(db, svc, name="베이직플랜")
    p_pro = await create_plan(db, svc, name="프로플랜")
    await create_subscription(db, cipher, svc, p_basic, external_user_id="sub-basic")
    await create_subscription(db, cipher, svc, p_pro, external_user_id="sub-pro")
    admin, pw = await create_user(db, role="SYSTEM_ADMIN")
    await admin_login(client, admin.email, pw)
    def tbody(h): return h[h.find("<tbody>"):]
    body = tbody((await client.get("/admin/subscriptions?plan_name=베이직플랜")).text)
    assert "sub-basic" in body and "sub-pro" not in body
    # 요금제 select + 서비스 선택 시 그 서비스 요금제 노출
    html = (await client.get(f"/admin/subscriptions?service_id={svc.id}")).text
    assert 'name="plan_name"' in html and "베이직플랜" in html and "프로플랜" in html
```

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py::test_subscriptions_plan_filter -x -q`

- [ ] **Step 3: 쿼리·옵션·필터키** — `app/admin/routes/subscriptions.py`
import: `from app.admin.filters import plan_name_options`.
`_build_subscriptions_query`에 plan_name 필터 추가(status 필터 다음):
```python
    if pp.filters.get("plan_name"):
        base = base.where(Plan.name == pp.filters["plan_name"])
```
`subscriptions_list`와 `subscriptions_export`의 `filter_keys`에 `"plan_name"` 추가(둘 다).
`subscriptions_list` render 직전에 옵션 빌드 + 전달:
```python
    plan_options = await plan_name_options(db, scope, service_filter)
```
render_list kwargs에 `plan_filter=pp.filters.get("plan_name", ""), plan_options=plan_options` 추가.

- [ ] **Step 4: 템플릿** — `subscriptions/_table.html`의 toolbar extra_selects에서 service_id 다음에 요금제 추가:
```jinja
   ('service_id', service_options, service_filter),
   ('plan_name', plan_options, plan_filter),
   ('status', [('','전체 상태'),('TRIAL','TRIAL'),('ACTIVE','ACTIVE'),('PAST_DUE','PAST_DUE'),('SUSPENDED','SUSPENDED'),('CANCELED','CANCELED'),('EXPIRED','EXPIRED')], status_filter)],
```

- [ ] **Step 5: 통과 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py -q` → PASS(구독 목록·필터 회귀 포함).

- [ ] **Step 6: 커밋**
```bash
git add app/admin/routes/subscriptions.py app/admin/templates/subscriptions/_table.html tests/e2e/test_admin_operations.py
git commit -m "feat(subscriptions): 목록에 서비스 연동 요금제 필터

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 결제이력 요금제 필터 (구독결제만)

**Files:**
- Modify: `app/admin/routes/subscriptions.py`, `app/admin/templates/payments/list.html`
- Test: `tests/e2e/test_admin_operations.py`

- [ ] **Step 1: 실패 테스트 추가** — `tests/e2e/test_admin_operations.py` 끝에:
```python
async def test_payments_plan_filter_subscription_only(client, db, redis_client, cipher):
    from app.core.clock import utcnow
    from app.models import Payment, PaymentKind, PaymentStatus, PaymentType
    svc, _, _ = await create_service(db, cipher, name="결제요금제서비스")
    plan = await create_plan(db, svc, name="요금제P")
    sub = await create_subscription(db, cipher, svc, plan, external_user_id="pf-sub")
    db.add(Payment(subscription_id=sub.id, service_id=svc.id, external_user_id="pf-sub",
                   order_id="pf-sub-pay", amount=1000, payment_type=PaymentType.RENEWAL,
                   kind=PaymentKind.SUBSCRIPTION, status=PaymentStatus.DONE,
                   idempotency_key="pf-sub-pay", requested_at=utcnow()))
    db.add(Payment(subscription_id=None, service_id=svc.id, external_user_id="pf-oo",
                   order_id="pf-oo-pay", amount=2000, payment_type=PaymentType.ONE_OFF,
                   kind=PaymentKind.ONE_OFF, status=PaymentStatus.DONE,
                   idempotency_key="pf-oo-pay", requested_at=utcnow()))
    await db.commit()
    admin, pw = await create_user(db, role="SYSTEM_ADMIN")
    await admin_login(client, admin.email, pw)
    def tbody(h): return h[h.find("<tbody>"):]
    body = tbody((await client.get("/admin/payments?plan_name=요금제P")).text)
    assert "pf-sub-pay" in body and "pf-oo-pay" not in body   # 일반결제 제외
    html = (await client.get(f"/admin/payments?service_id={svc.id}")).text
    assert 'name="plan_name"' in html and "요금제P" in html
```

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py::test_payments_plan_filter_subscription_only -x -q`

- [ ] **Step 3: 쿼리·옵션·필터키** — `app/admin/routes/subscriptions.py`
`_build_payments_query`의 base에 Plan outerjoin 추가:
```python
    base = (select(Payment, Subscription, Service)
            .outerjoin(Subscription, Payment.subscription_id == Subscription.id)
            .outerjoin(Plan, Subscription.plan_id == Plan.id)
            .join(Service, Payment.service_id == Service.id))
```
status 필터 근처에 plan_name 추가:
```python
    if pp.filters.get("plan_name"):
        base = base.where(Plan.name == pp.filters["plan_name"])
```
`payments_list`와 `payments_export`의 `filter_keys`에 `"plan_name"` 추가(둘 다).
`payments_list` render 직전:
```python
    plan_options = await plan_name_options(db, scope, service_filter)
```
render kwargs에 `plan_filter=pp.filters.get("plan_name", ""), plan_options=plan_options` 추가.
(`Plan`은 이미 import됨. `plan_name_options`는 Task 3 시점에 import되어 있음.)

- [ ] **Step 4: 템플릿** — `payments/list.html` toolbar extra_selects에서 service_id 다음 요금제 추가(kind 앞):
```jinja
   [('service_id', service_options, service_filter),
    ('plan_name', plan_options, plan_filter),
    ('kind', [('','전체 종류'),('SUBSCRIPTION','구독'),('ONE_OFF','일반')], kind_filter),
    ('status', [('','전체 상태'),('DONE','DONE'),('FAILED','FAILED'),('PENDING','PENDING'),('CANCELED','CANCELED')], status_filter)],
```

- [ ] **Step 5: 통과 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py -q` → PASS.

- [ ] **Step 6: 커밋**
```bash
git add app/admin/routes/subscriptions.py app/admin/templates/payments/list.html tests/e2e/test_admin_operations.py
git commit -m "feat(payments): 결제이력에 서비스 연동 요금제 필터(구독결제만)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: enum 색 배지 (종류·유형) + 도넛 색 분리

**Files:**
- Modify: `app/static/admin.css`, `app/admin/templates/payments/list.html`, `app/admin/templates/settlement/index.html`, `app/admin/templates/services/_oneoff_table.html`, `app/services/dashboard.py`
- Test: `tests/e2e/test_admin_operations.py`

- [ ] **Step 1: 실패 테스트 추가** — `tests/e2e/test_admin_operations.py` 끝에:
```python
async def test_payment_kind_type_badges(client, db, redis_client, cipher):
    from app.core.clock import utcnow
    from app.models import Payment, PaymentKind, PaymentStatus, PaymentType
    svc, _, _ = await create_service(db, cipher)
    db.add(Payment(subscription_id=None, service_id=svc.id, external_user_id="b-oo",
                   order_id="badge-oo", amount=1000, payment_type=PaymentType.ONE_OFF,
                   kind=PaymentKind.ONE_OFF, status=PaymentStatus.DONE,
                   idempotency_key="badge-oo", requested_at=utcnow()))
    await db.commit()
    admin, pw = await create_user(db, role="SYSTEM_ADMIN")
    await admin_login(client, admin.email, pw)
    html = (await client.get("/admin/payments")).text
    assert "badge-ONE_OFF" in html        # 종류 색 배지
    assert "badge badge-ONE_OFF" in html   # 유형도 배지(payment_type=ONE_OFF)
```

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py::test_payment_kind_type_badges -x -q`

- [ ] **Step 3: CSS 추가** — `app/static/admin.css`의 badge 블록(`.badge-EXPIRED...` 줄 다음)에 종류/유형 색 추가:
```css
.badge-SUBSCRIPTION { color: #1C5FA8; background: var(--card-blue); }
.badge-SUBSCRIPTION::before { background: var(--status-info); }
.badge-ONE_OFF { color: #0E7C7B; background: rgba(20,184,166,.14); }
.badge-ONE_OFF::before { background: #14B8A6; }
.badge-FIRST { color: #1A7F4B; background: rgba(31,193,107,.12); }
.badge-FIRST::before { background: var(--status-online); }
.badge-RENEWAL { color: #4C49C6; background: rgba(99,91,255,.12); }
.badge-RENEWAL::before { background: #635BFF; }
.badge-RETRY { color: #B26A00; background: rgba(255,193,130,.18); }
.badge-RETRY::before { background: var(--status-warning); }
```

- [ ] **Step 4: 종류/유형 표시를 배지로** — 다음 위치를 교체:
  - `payments/list.html`: 종류 `<td><span class="badge badge-{{ p.kind }}">{{ '구독' if p.kind == 'SUBSCRIPTION' else '일반' }}</span></td>`, 유형 `<td><span class="badge badge-{{ p.payment_type }}">{{ p.payment_type }}</span></td>`
  - `settlement/index.html` 서비스별 건별 표: 유형 `<td><span class="badge badge-{{ p.payment_type }}">{{ p.payment_type }}</span></td>`, 종류 `<td><span class="badge badge-{{ p.kind }}">{{ '구독' if p.kind == 'SUBSCRIPTION' else '일반' }}</span></td>`
  - `services/_oneoff_table.html`: 종류 컬럼이 있으면 동일 적용(없으면 생략).

- [ ] **Step 5: 도넛 EXPIRED 색 분리** — `app/services/dashboard.py` `_STATUS_COLOR`에서 EXPIRED를 CANCELED(회색)와 구별되게:
```python
    "CANCELED": "var(--black-20)",
    "EXPIRED": "var(--accent-indigo)",
```

- [ ] **Step 6: 통과 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py tests/e2e/test_dashboard_page.py -q` → PASS.

- [ ] **Step 7: 커밋**
```bash
git add app/static/admin.css app/admin/templates/payments/list.html app/admin/templates/settlement/index.html app/admin/templates/services/_oneoff_table.html app/services/dashboard.py tests/e2e/test_admin_operations.py
git commit -m "feat(ui): 결제 종류·유형 색 배지 + 도넛 만료 색 분리

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 공용 결제 상세 `/admin/payments/{id}`

**Files:**
- Modify: `app/admin/routes/subscriptions.py`, `app/admin/templates/payments/list.html`
- Create: `app/admin/templates/payments/detail.html`
- Test: `tests/e2e/test_admin_operations.py`

- [ ] **Step 1: 실패 테스트 추가** — `tests/e2e/test_admin_operations.py` 끝에:
```python
async def test_payment_detail_page_and_scope(client, db, redis_client, cipher):
    from app.core.clock import utcnow
    from app.models import Payment, PaymentKind, PaymentStatus, PaymentType
    svc_a, _, _ = await create_service(db, cipher, name="상세A")
    svc_b, _, _ = await create_service(db, cipher, name="상세B")
    p = Payment(subscription_id=None, service_id=svc_a.id, external_user_id="det-u",
                order_id="det-pay", amount=4200, payment_type=PaymentType.ONE_OFF,
                kind=PaymentKind.ONE_OFF, status=PaymentStatus.DONE,
                idempotency_key="det-pay", requested_at=utcnow(), approved_at=utcnow())
    db.add(p); await db.commit(); await db.refresh(p)
    admin, pw = await create_user(db, role="SYSTEM_ADMIN")
    await admin_login(client, admin.email, pw)
    html = (await client.get(f"/admin/payments/{p.id}")).text
    assert "det-pay" in html and "4,200" in html and "상세A" in html
    # 매니저 스코프: 타 서비스 결제 404
    mgr, pw2 = await create_user(db, role="SERVICE_MANAGER", service_id=svc_b.id)
    await admin_login(client, mgr.email, pw2)
    resp = await client.get(f"/admin/payments/{p.id}")
    assert resp.status_code == 404
```

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py::test_payment_detail_page_and_scope -x -q`

- [ ] **Step 3: 라우트** — `app/admin/routes/subscriptions.py`의 `payments_export` 다음, `payments_list` 또는 적절한 위치(정적 `/payments/export.xlsx` 뒤)에:
```python
@router.get("/payments/{payment_id}")
async def payment_detail(payment_id: uuid.UUID, request: Request,
                         ctx: AdminContext = Depends(require_any),
                         db: AsyncSession = Depends(get_db)):
    payment = await db.get(Payment, payment_id)
    scope = _scope(ctx)
    if payment is None or (scope is not None and payment.service_id not in scope):
        raise NotFoundError("결제를 찾을 수 없습니다")
    service = await db.get(Service, payment.service_id)
    sub = (await db.get(Subscription, payment.subscription_id)
           if payment.subscription_id else None)
    return render(request, "payments/detail.html", ctx=ctx,
                  payment=payment, service=service, sub=sub)
```
(`NotFoundError`, `render`, `Service`, `Subscription`, `Payment`, `_scope`는 이 파일에서 이미 사용 중.)

- [ ] **Step 4: 템플릿** — `app/admin/templates/payments/detail.html`:
```jinja
{% extends "base.html" %}
{% block title %}결제 상세{% endblock %}
{% block crumb %}<a href="/admin/payments">결제</a> · 상세{% endblock %}
{% block content %}
<div class="page-head"><h1>결제 상세</h1>
  <span class="muted" style="font-size:12px;font-family:ui-monospace,monospace">{{ payment.order_id }}</span></div>
<div class="card">
  <table>
    <tbody>
      <tr><th style="text-align:left;width:160px">주문번호</th>
          <td style="font-family:ui-monospace,monospace">{{ payment.order_id }}</td></tr>
      <tr><th style="text-align:left">종류</th>
          <td><span class="badge badge-{{ payment.kind }}">{{ '구독' if payment.kind == 'SUBSCRIPTION' else '일반' }}</span></td></tr>
      <tr><th style="text-align:left">결제유형</th>
          <td><span class="badge badge-{{ payment.payment_type }}">{{ payment.payment_type }}</span></td></tr>
      <tr><th style="text-align:left">서비스</th><td>{{ service.name }}</td></tr>
      <tr><th style="text-align:left">사용자</th><td>{{ payment.external_user_id or '-' }}</td></tr>
      <tr><th style="text-align:left">금액</th><td style="font-weight:600">{{ "{:,}".format(payment.amount) }}원</td></tr>
      <tr><th style="text-align:left">상태</th>
          <td><span class="badge badge-{{ payment.status }}">{{ payment.status }}</span></td></tr>
      <tr><th style="text-align:left">실패 코드</th><td class="muted">{{ payment.failure_code or '-' }}</td></tr>
      <tr><th style="text-align:left">실패 메시지</th><td class="muted">{{ payment.failure_message or '-' }}</td></tr>
      <tr><th style="text-align:left">요청 시각</th><td>{{ payment.requested_at|kst("%Y-%m-%d %H:%M:%S") }}</td></tr>
      <tr><th style="text-align:left">승인 시각</th><td>{{ payment.approved_at|kst("%Y-%m-%d %H:%M:%S") }}</td></tr>
      <tr><th style="text-align:left">토스 결제키</th>
          <td style="font-family:ui-monospace,monospace;font-size:12px">{{ payment.toss_payment_key or '-' }}</td></tr>
      <tr><th style="text-align:left">연결 구독</th>
          <td>{% if sub %}<a href="/admin/subscriptions/{{ sub.id }}">{{ sub.external_user_id }} ›</a>{% else %}-{% endif %}</td></tr>
    </tbody>
  </table>
</div>
{% if payment.raw_response %}
<div class="card">
  <div class="block-head"><h2>원본 응답</h2></div>
  <pre style="overflow:auto;font-size:12px;background:var(--black-4);padding:12px;border-radius:8px">{{ payment.raw_response|tojson(indent=2) }}</pre>
</div>
{% endif %}
{% endblock %}
```

- [ ] **Step 5: 목록 연결** — `payments/list.html`의 주문번호 셀을 상세 링크로:
```jinja
      <td style="font-family:ui-monospace,monospace;font-size:12px"><a href="/admin/payments/{{ p.id }}">{{ p.order_id }}</a></td>
```

- [ ] **Step 6: 통과 확인** — Run: `uv run pytest tests/e2e/test_admin_operations.py -q` → PASS(결제 상세·스코프 + 목록 회귀).

- [ ] **Step 7: 커밋**
```bash
git add app/admin/routes/subscriptions.py app/admin/templates/payments/detail.html app/admin/templates/payments/list.html tests/e2e/test_admin_operations.py
git commit -m "feat(payments): 공용 결제 상세 페이지 + 목록 연결

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 정산 — 요금제 필터 + 구독/일반 분리금액(건수) + 일반결제 상세

**Files:**
- Modify: `app/services/settlement.py`, `app/admin/routes/settlement.py`, `app/admin/templates/settlement/index.html`
- Test: `tests/integration/test_settlement.py`, `tests/e2e/test_settlement_page.py`

- [ ] **Step 1: 통합 실패 테스트** — `tests/integration/test_settlement.py` 끝에:
```python
async def test_settlement_split_counts_and_plan_filter(db, cipher):
    from datetime import datetime, timezone
    from app.models import Payment, PaymentKind, PaymentStatus, PaymentType
    UTC = timezone.utc
    svc, _, _ = await create_service(db, cipher, name="정산011")
    plan = await create_plan(db, svc, name="정산플랜")
    sub = await create_subscription(db, cipher, svc, plan, external_user_id="u")
    when = datetime(2026, 5, 10, tzinfo=UTC)
    db.add(Payment(subscription_id=sub.id, service_id=svc.id, external_user_id="u",
                   order_id="s011-sub", amount=10000, payment_type=PaymentType.RENEWAL,
                   kind=PaymentKind.SUBSCRIPTION, status=PaymentStatus.DONE,
                   idempotency_key="s011-sub", requested_at=when, approved_at=when))
    db.add(Payment(subscription_id=None, service_id=svc.id, external_user_id="u2",
                   order_id="s011-oo", amount=3000, payment_type=PaymentType.ONE_OFF,
                   kind=PaymentKind.ONE_OFF, status=PaymentStatus.DONE,
                   idempotency_key="s011-oo", requested_at=when, approved_at=when))
    await db.commit()
    start, end = datetime(2026,5,1,tzinfo=UTC), datetime(2026,6,1,tzinfo=UTC)
    count, amount, rows = await settlement_summary(db, None, start, end)
    row = next(r for r in rows if r.service_name == "정산011")
    assert row.sub_count == 1 and row.one_off_count == 1     # 분리 건수
    # 요금제 필터 → 구독결제만
    count2, amount2, rows2 = await settlement_summary(db, None, start, end, plan_name="정산플랜")
    assert amount2 == 10000 and count2 == 1                  # 일반결제 제외
```

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/integration/test_settlement.py -k split_counts -x -q`

- [ ] **Step 3: settlement_summary 확장** — `app/services/settlement.py`
`SettlementRow`에 필드 추가:
```python
    sub_count: int = 0
    one_off_count: int = 0
```
(`amount`/`sub_amount`/`one_off_amount` 뒤. 기본값 부여로 기존 생성 호환.)
`settlement_summary` 시그니처에 `plan_name: str | None = None` 추가. count 분리 + plan join:
```python
async def settlement_summary(db, scope, start, end, plan_name=None):
    amount_sum = func.coalesce(func.sum(Payment.amount), 0)
    sub_sum = func.coalesce(func.sum(case((Payment.kind == PaymentKind.SUBSCRIPTION, Payment.amount), else_=0)), 0)
    oo_sum = func.coalesce(func.sum(case((Payment.kind == PaymentKind.ONE_OFF, Payment.amount), else_=0)), 0)
    sub_cnt = func.coalesce(func.sum(case((Payment.kind == PaymentKind.SUBSCRIPTION, 1), else_=0)), 0)
    oo_cnt = func.coalesce(func.sum(case((Payment.kind == PaymentKind.ONE_OFF, 1), else_=0)), 0)
    q = (select(Service.id, Service.name, func.count(Payment.id), amount_sum, sub_sum, oo_sum, sub_cnt, oo_cnt)
         .select_from(Payment)
         .join(Service, Payment.service_id == Service.id)
         .where(Payment.status == PaymentStatus.DONE)
         .group_by(Service.id, Service.name)
         .order_by(amount_sum.desc(), Service.name))
    if plan_name:
        q = (q.join(Subscription, Payment.subscription_id == Subscription.id)
              .join(Plan, Subscription.plan_id == Plan.id)
              .where(Plan.name == plan_name))
    if start:
        q = q.where(Payment.approved_at >= start)
    if end:
        q = q.where(Payment.approved_at < end)
    if scope is not None:
        q = q.where(Payment.service_id.in_(scope))
    rows = [SettlementRow(sid, name, int(c), int(a), int(sa), int(oo), int(sc), int(oc))
            for sid, name, c, a, sa, oo, sc, oc in (await db.execute(q)).all()]
    return (sum(r.count for r in rows), sum(r.amount for r in rows), rows)
```
import 추가: `from app.models import ... Plan, Subscription ...`(없으면).

- [ ] **Step 4: e2e 실패 테스트** — `tests/e2e/test_settlement_page.py` 끝에:
```python
async def test_settlement_shows_split_and_oneoff_detail(client, db, redis_client, cipher):
    from app.models import Payment, PaymentKind, PaymentStatus, PaymentType
    svc, _, _ = await create_service(db, cipher, name="정산상세S")
    when = datetime(2026, 5, 10, tzinfo=UTC)
    p = Payment(subscription_id=None, service_id=svc.id, external_user_id="oo-u",
                order_id="s-oo-det", amount=3000, payment_type=PaymentType.ONE_OFF,
                kind=PaymentKind.ONE_OFF, status=PaymentStatus.DONE,
                idempotency_key="s-oo-det", requested_at=when, approved_at=when)
    db.add(p); await db.commit(); await db.refresh(p)
    admin, pw = await create_user(db, role="SYSTEM_ADMIN")
    await admin_login(client, admin.email, pw)
    # 전체 모드: 구독/일반 분리 금액·건수
    html = (await client.get("/admin/settlement?from=2026-05-01&to=2026-05-31")).text
    assert "구독 정산금액" in html and "일반결제 정산금액" in html
    # 서비스별 모드: 일반결제 상세보기 → 결제상세
    html2 = (await client.get(
        f"/admin/settlement?from=2026-05-01&to=2026-05-31&service_id={svc.id}")).text
    assert f"/admin/payments/{p.id}" in html2
    # 요금제 select 노출
    assert 'name="plan_name"' in html2
```
(파일 상단 `UTC = timezone.utc`, `from datetime import datetime, timezone` 존재 확인 — 없으면 추가.)

- [ ] **Step 5: 실패 확인** — Run: `uv run pytest tests/e2e/test_settlement_page.py -k "split_and_oneoff" -x -q`

- [ ] **Step 6: settlement_view + export + 템플릿** — `app/admin/routes/settlement.py`
`_settlement_context` 또는 `settlement_view`에서 `plan_name = pp.filters.get("plan_name") or None` 읽기. filter_keys에 `"plan_name"` 추가(view + export 모두).
`settlement_summary(...)` 호출에 `plan_name=plan_name` 전달(view + export). 건별 쿼리(view + export)에도 plan_name이면 `.join(Subscription).join(Plan).where(Plan.name==plan_name)` 적용(단, 건별 base는 이미 `outerjoin(Subscription)` — plan_name일 때만 inner join 추가하거나 where로 제한; ONE_OFF 제외 의도).
옵션: `plan_options = await plan_name_options(db, scope, raw_sid)` (import `from app.admin.filters import plan_name_options`). render에 `plan_options=plan_options, plan_filter=plan_name or ""` 전달.
분리 합계 계산(view): `sub_total = sum(r.sub_amount for r in rows)`, `one_off_total = sum(r.one_off_amount for r in rows)`, `sub_count = sum(r.sub_count for r in rows)`, `one_off_count = sum(r.one_off_count for r in rows)` → render 전달.

`settlement/index.html`:
- 서비스 select 다음 요금제 select 추가(onchange 재요청):
```jinja
  <select name="plan_name" onchange="this.form.requestSubmit()">
    {% for value, label in plan_options %}
    <option value="{{ value }}" {{ 'selected' if plan_filter == value }}>{{ label }}</option>
    {% endfor %}
  </select>
```
- 전체 정산 금액 stat 아래에 분리 표시(stats 블록 다음):
```jinja
<div class="stats" style="margin-bottom:16px">
  <div class="stat stat-tint-1">
    <span class="stat-label">구독 정산금액</span>
    <div class="stat-row"><span class="stat-value">{{ "{:,}".format(sub_total) }}원</span>
      <span class="muted">{{ sub_count }}건</span></div>
  </div>
  <div class="stat stat-tint-2">
    <span class="stat-label">일반결제 정산금액</span>
    <div class="stat-row"><span class="stat-value">{{ "{:,}".format(one_off_total) }}원</span>
      <span class="muted">{{ one_off_count }}건</span></div>
  </div>
</div>
```
- 서비스별 모드 건별 표 상세보기 셀(일반결제):
```jinja
      <td>{% if sub %}<a class="btn btn-sm btn-ghost" href="/admin/subscriptions/{{ sub.id }}">상세보기</a>{% else %}<a class="btn btn-sm btn-ghost" href="/admin/payments/{{ p.id }}">상세보기</a>{% endif %}</td>
```
- 엑셀 링크에 plan_name 유지: `...&service_id=...{% if plan_filter %}&plan_name={{ plan_filter }}{% endif %}`.

- [ ] **Step 7: 통과 확인** — Run: `uv run pytest tests/integration/test_settlement.py tests/e2e/test_settlement_page.py tests/e2e/test_list_export.py -q` → PASS.

- [ ] **Step 8: 커밋**
```bash
git add app/services/settlement.py app/admin/routes/settlement.py app/admin/templates/settlement/index.html tests/integration/test_settlement.py tests/e2e/test_settlement_page.py
git commit -m "feat(settlement): 요금제 필터 + 구독/일반 분리금액·건수 + 일반결제 상세

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 대시보드 도넛 전체 상태(만료 포함)

**Files:**
- Modify: `app/services/dashboard.py`, `app/admin/templates/dashboard.html`
- Test: `tests/integration/test_dashboard.py`, `tests/e2e/test_dashboard_page.py`

- [ ] **Step 1: 통합 실패 테스트** — `tests/integration/test_dashboard.py`의 `test_status_donut_matches_current_subs`를 만료 포함 버전으로 교체(또는 신규):
```python
async def test_status_donut_includes_expired(db, cipher):
    svc, _, _ = await create_service(db, cipher)
    plan = await create_plan(db, svc)
    await create_subscription(db, cipher, svc, plan, external_user_id="a1", status="ACTIVE")
    await create_subscription(db, cipher, svc, plan, external_user_id="x1", status="EXPIRED")
    data = await build_dashboard(db, None)
    labels = {row["label"]: row["value"] for row in data.status_breakdown}
    assert labels.get("활성") == 1
    assert labels.get("만료") == 1           # 만료가 도넛에 포함
    assert sum(data.status_breakdown[i]["value"] for i in range(len(data.status_breakdown))) == 2
```
(기존 `test_status_donut_matches_current_subs`가 "만료 제외"를 단언하면 제거/교체.)

- [ ] **Step 2: 실패 확인** — Run: `uv run pytest tests/integration/test_dashboard.py -k "expired" -x -q`

- [ ] **Step 3: build_dashboard 수정** — `app/services/dashboard.py`의 counts 집계를 전체 상태로:
```python
    # 전체 상태별 집계 — 도넛에 만료(EXPIRED) 포함(요청 011)
    counts = {s: 0 for s in _STATUS_ORDER}
    rows = (await db.execute(_scoped(
        select(Subscription.status, func.count()).group_by(Subscription.status),
        scope, Subscription.service_id))).all()
    for status, n in rows:
        counts[status] = n
```
(`.where(_open_subs_cond(now))` 제거.)

- [ ] **Step 4: 템플릿 부제** — `dashboard.html` 도넛 블록 부제를 전체 상태로:
```jinja
    <div class="block-head"><h2>구독 상태</h2>
      <span class="muted" style="font-size:12px">전체 상태(상태별 비율) · 클릭 시 상세</span></div>
```
(중앙 라벨 "현재"는 "전체"로 바꿔도 됨 — `_charts.html` donut 중앙 텍스트 "현재" → "전체". 단, donut 매크로는 공용이므로 다른 사용처 없으면 변경; 현재 donut은 대시보드 전용.)

- [ ] **Step 5: e2e 갱신** — `tests/e2e/test_dashboard_page.py`에서 도넛 관련 단언이 "현재 이용 중"을 검사하면 "전체 상태"로 갱신. 만료 구독 seed 추가해 도넛에 "만료" 표시 확인(선택).

- [ ] **Step 6: 통과 확인** — Run: `uv run pytest tests/integration/test_dashboard.py tests/e2e/test_dashboard_page.py -q` → PASS.

- [ ] **Step 7: 커밋**
```bash
git add app/services/dashboard.py app/admin/templates/dashboard.html app/admin/templates/_charts.html tests/integration/test_dashboard.py tests/e2e/test_dashboard_page.py
git commit -m "feat(dashboard): 구독상태 도넛에 만료 포함(전체 상태)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 전체 검증 + 최종 리뷰
- [ ] **Step 1: 전체 테스트** — Run: `uv run pytest -q` → 전체 PASS.
- [ ] **Step 2: 잔여 확인**
  - `grep -rn "plan_name_options" app/admin/routes` → plans·subscriptions(2)·settlement에서 사용.
  - `grep -rn "badge-ONE_OFF\|badge-SUBSCRIPTION\|badge-RENEWAL" app/admin/templates` → 종류/유형 배지 적용 확인.
  - `grep -rn "_open_subs_cond" app/services/dashboard.py` → status_breakdown 집계에는 없음(도넛 전체 상태).
- [ ] **Step 3: 커밋(잔여 정리 시)**
```bash
git add -A app tests
git commit -m "test: 요청011 잔여 정리"
```

## 변경하지 않는 것 (스펙 동일)
- 도메인/모델/마이그레이션(Payment plan_id 미추가), 외부 API, 엑셀 export 라벨.
