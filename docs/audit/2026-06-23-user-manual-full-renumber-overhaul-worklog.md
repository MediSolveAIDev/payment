# 2026-06-23 — user_manual 전면 정비(전체 재번호 + 기능 보강 + placeholder 이미지)

## 요청
`docs/user_manual` 전체(사용자 12 + 개발자 8 = 20문서)를 ① 현재 기능 누락 없이 작성, ② 넘버링 정확히 맞춤, ③ 이미지 필요한 곳에 임시 placeholder 삽입(사용자가 캡처해 교체).

## 결정(사용자 확인)
- 범위: 전체 20문서.
- 넘버링: **파일명까지 사이드바 표시 순서대로 재정렬**(파일번호 = 표시번호 = H1번호 일치).

## 새 번호 체계 (old → new)
사용자 매뉴얼(00~11): 00 개요 · 01 콘솔 · 02 서비스관리(←19) · 03 카드(←02) · 04 구독(←03) · 05 요금제(←04) · 06 결제/환불(←05) · 07 계정(←06) · 08 설정(←07) · 09 감사로그(←08) · 10 대시보드(←09) · 11 상태사전(←18)
개발자 매뉴얼(12~19): 12 설치/배포(←10) · 13 서비스API(←11) · 14 기능-카드(←12) · 15 기능-구독(←13) · 16 기능-결제(←14) · 17 기능-알림(←15) · 18 어드민화면(←16) · 19 샘플서비스(←17)

## 작업
1. **기계적 재번호(스크립트)**: 20개 .md 2단계 `git mv`(충돌 회피), 전 문서 상호 `.md` 링크 **117건 일괄 치환**(1패스, 연쇄 재치환 방지), `build.py`의 DOCS 리스트 순서·파일명 갱신. 구 `.html` 삭제 후 재빌드.
2. **문서별 정비(병렬 에이전트 20문서)**: 각 문서 H1을 새 번호로, 내부 `##`/`###` 섹션을 `<문서번호>.x`로 재번호. **실제 코드 대조로 기능 누락·부정확(stale file:line) 보강.** 주요 보강:
   - 02 서비스관리: 알림 URL 저장·테스트전송, 키 재발급, 토스 시크릿키, 상세 5탭 등
   - 11 상태사전: `BillingCycle.MINUTE` 누락 보강(코드 enum 대조)
   - 12 설치/배포: 환경변수 전수(약 30개) 반영, 최초 관리자 `create-admin`
   - 13 서비스API: 누락 엔드포인트 3종(GET /services, GET /plans, POST /webhooks/toss) 추가
   - 14~17 기능: 서비스별 토스 클라이언트(`for_service`), 분산락, 타임아웃 재조회, 알림 17종 이벤트·payload 등 코드 대조 정정
   - 18 어드민화면: 가이드 페이지 추가, 로그인 자동입력 `environment == "dev"` 반영
3. **placeholder 이미지**: 본문 참조 이미지 28개 중 기존 25개 재사용, 신규 3개(`02-services-list.png`, `03-card-toggle.png`, `11-subscription-state.png`)는 "스크린샷 자리 — 캡처 후 교체" 안내가 담긴 1280×720 PNG placeholder 생성(Pillow). 사용자가 같은 파일명으로 덮어쓰면 즉시 반영.

## 검증(전수)
- H1 번호 0~19 사이드바 순서와 완전 일치 ✓
- 모든 `##`/`###` 섹션 선두번호 = 문서번호 (불일치 0건, 코드블록 내 `#` 제외) ✓
- 본문 참조 이미지 누락 0건 ✓
- 문서 간 `.md` 상호링크 깨짐 0건 ✓
- `build.py` 재빌드 정상(20문서 + index.html) ✓
- git rename 정상 감지(RM), 구 `.html` 삭제·신 `.html` 생성 ✓

## 비고
- 로그인 페이지의 "📚 전체 매뉴얼" 링크(`/user-manual/`)가 이 사이트를 서빙.
- 세부 문서별 워크로그는 일부 에이전트가 개별 생성(02/08/10/13/19). 본 문서가 통합 기록.
