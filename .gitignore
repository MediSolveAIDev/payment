# macOS 잡파일
.DS_Store

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
.env.*
!.env.example

# Test/coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# DB 백업 덤프(민감정보 — 커밋 금지)
backups/

# TLS 인증서/개인키(커밋 금지 — 런타임에 배치/발급, .gitkeep만 추적)
docker/nginx/certs/*.pem
